# Logging
Just prints out things using ***cout***
Contains next logging functions:

# Functions:
    Debug
    Warning
    Error


# Example of usage:
```c++
salt::Logging::Debug("message")
salt::Logging::Warning("message")
salt::Logging::Error("message")
```